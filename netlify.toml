[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Environment variables (set these in Netlify dashboard)
# NEXT_PUBLIC_CHAT_APP_ADDRESS = "your_contract_address_here"
